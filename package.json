{
  "name": "brady-runs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "parse": "node scripts/parse-fit-files.js",
    "test": "node scripts/test-calculations.js",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "fit-file-parser": "^1.9.0",
    "leaflet": "^1.9.4",
    "pako": "^2.1.0"
  }
}
